<?xml version="1.0" encoding="utf-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for page in collections.all %}
    <url>
        <loc>{{ page.url | url | absoluteUrl(site.url) }}</loc>
        {% if page.data.lastmod %}
        <lastmod>{{ page.data.lastmod | htmlDateString }}</lastmod>
        {% endif %}
        {% if page.data.changefreq %}
        <changefreq>{{ page.data.changefreq }}</changefreq>
        {% endif %}
        {% if page.data.priority %}
        <priority>{{ page.data.priority }}</priority>
        {% endif %}
    </url>
    {% endfor %}
</urlset>