<!DOCTYPE html>
<html lang="en">
	{% include "partials/_site-head.njk" %}
	<body>
		{% include "partials/_site-header.njk" %}
		<main>
			<div class="center flow">
				<h1>{{ title }}</h1>
				{{ content | safe }}
			</div>
		</main>
	</body>
</html>
