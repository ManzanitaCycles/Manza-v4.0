<section class="sidebar">
    {% image image, alt, "(min-width: 1560px) 900px, (min-width: 1080px) calc(83.7vw - 389px), (min-width: 1020px) 900px, 90.29vw", "eager", "high" %}
    <div class="stack">
        <p class="fine-print">{{ subTitle }}</p>
        <h1 class="m-top-0">{{ title }}</h1>
        <p class="muted m-top-2xs">{{ desc }}</p>
        <p><a href="{{ depositLink }}" class="button">Place deposit</a></p>
    </div>
</section>